# 🔄 Sorting Algorithms

This folder contains implementations of fundamental **sorting algorithms** in **C++**. Each algorithm follows a unique approach to arranging data in ascending order and helps build a strong understanding of algorithm design and problem-solving.

---

## 📂 Implemented Algorithms

### 🔹 Bubble Sort

Bubble Sort repeatedly compares adjacent elements and swaps them if they are in the wrong order. With each pass, the largest unsorted element moves to its correct position.

### 🔹 Selection Sort

Selection Sort divides the array into sorted and unsorted portions. It repeatedly finds the smallest element from the unsorted portion and places it at the beginning of the sorted portion.

### 🔹 Insertion Sort

Insertion Sort builds a sorted array one element at a time by inserting each new element into its correct position among the previously sorted elements.

### 🔹 Merge Sort

Merge Sort follows the divide-and-conquer approach. It recursively splits the array into smaller subarrays, sorts them, and merges them back together to produce a fully sorted array.

### 🔹 Quick Sort

Quick Sort also uses the divide-and-conquer technique. It selects a pivot element, partitions the array around the pivot, and recursively sorts the resulting subarrays.

### 🔹 Quick Sort (Alternative Implementation)

This file contains another implementation of Quick Sort using a different partitioning strategy, demonstrating an alternative way to organize the recursive sorting process.

---

## 🎯 Purpose

The implementations in this folder are intended for educational purposes and help in:

* Understanding the logic behind different sorting techniques.
* Comparing how various algorithms approach the same problem.
* Improving algorithmic thinking and programming skills.
* Building a strong foundation in Data Structures and Algorithms.

---

## 💻 Language

* **C++**

---

This folder is part of my **Data Structures & Algorithms** repository and will continue to expand as I learn and implement additional sorting techniques.
